define([
    'underscore'
  ], function (_) {
  var Currency;

  /**
   * Creates a new currency.
   *
   * @constructor
   */
  Currency = function () {
  };

  _.extend(Currency.prototype, {
    // TODO: Add currency methods here
  });

  return Currency;
});
